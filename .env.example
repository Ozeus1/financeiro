# IMPORTANTE: Copie este arquivo para .env e configure com suas credenciais reais
# Nunca commite o arquivo .env no Git!

# Chave secreta (MUDE para algo único e seguro em produção!)
# Para gerar: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here-change-in-production

# URL do banco de dados PostgreSQL
# Formato: postgresql://usuario:senha@host:porta/nome_banco
DATABASE_URL=postgresql://postgres:sua_senha@localhost:5432/financeiro

# Ambiente de execução (development ou production)
FLASK_ENV=production
FLASK_DEBUG=False
